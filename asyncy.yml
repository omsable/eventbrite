commands:
  attendees:
    help: |
      Returns a list of attendees
    args:
      - name: event-id
        type: string
        help: The event id to query, defaults to env: EVENT_ID

env:
  OAUTH_TOKEN:
  EVENT_ID:
